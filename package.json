{
  "name": "@tbiegner99/security-monitor",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "setup": "tsc && node dist/setup.js",
    "dev": "tsc && node dist/index.js",
    "watch": "tsc --watch",
    "install-service": "sudo ./install-service.sh",
    "uninstall-service": "sudo ./uninstall-service.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "GPIO-based security monitoring system with MQTT and logging support",
  "dependencies": {
    "mqtt": "^5.3.4",
    "onoff": "^6.0.3"
  },
  "devDependencies": {
    "@types/mqtt": "^0.0.34",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
